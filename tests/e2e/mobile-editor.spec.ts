import { test } from '@playwright/test';

test.describe('mobile editor (375px viewport)', () => {
  test.skip('pinch-zoom updates cropTransform.scale', async () => {});
  test.skip('color change reflects within 100ms (visual)', async () => {});
  test.skip('build output contains no `konva` string in server chunks', async () => {});
});
