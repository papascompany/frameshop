/**
 * <FrameEditor> integration (Phase 4 — frontend dev).
 *
 * Spec: docs/specs/editor.md IT-01, IT-02.
 */

import { describe, it } from 'vitest';

describe('<FrameEditor>', () => {
  it.todo('mounts with default variant + initial canvas paint');
  it.todo('color change updates frame PNG src and price');
  it.todo('matte toggle shrinks photo area');
  it.todo('"add to cart" triggers preview upload + onConfirm');
  it.todo('invalid option combo rolls back to previous selection');
});
